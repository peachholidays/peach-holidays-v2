rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow Phase 3 & 4 Data Injection from MD Intelligence Tools
      allow read, write: if true;
    }
  }
}
